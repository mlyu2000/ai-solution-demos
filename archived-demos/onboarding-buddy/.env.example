# Application Environment Variables
# ==============================

# Proxy Configuration
# ------------------
# The URL of the Node.js proxy server (runs on port 3001 by default).
# This is used by the frontend to communicate with the proxy server.
# If you are running the proxy server on a different host or port, 
# create an .env file add this environment variable there with the updated value.
# VITE_PROXY_URL=http://localhost:3001
