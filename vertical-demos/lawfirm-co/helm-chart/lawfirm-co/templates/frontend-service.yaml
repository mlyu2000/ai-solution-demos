apiVersion: v1
kind: Service
metadata:
  name: {{ include "lawfirm.fullname" . }}-frontend
  labels:
    app.kubernetes.io/name: {{ include "lawfirm.fullname" . }}-frontend
    {{- include "lawfirm.labels" . | nindent 4 }}
spec:
  selector:
    app.kubernetes.io/name: {{ include "lawfirm.fullname" . }}-frontend
  ports:
    - protocol: TCP
      port: {{ .Values.frontend.service.port }}
      targetPort: {{ .Values.frontend.service.targetPort }}
  type: ClusterIP
