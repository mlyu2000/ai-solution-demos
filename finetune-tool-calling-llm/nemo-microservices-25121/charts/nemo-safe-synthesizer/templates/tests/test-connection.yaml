apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nemo-safe-synthesizer.fullname" . }}-test-connection"
  labels:
    {{- include "nemo-safe-synthesizer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nemo-safe-synthesizer.fullname" . }}:{{ .Values.service.port }}'] 