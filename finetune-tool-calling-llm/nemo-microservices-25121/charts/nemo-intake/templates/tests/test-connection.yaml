apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nemo-common.servicename" . }}-test-connection"
  labels:
    {{- include "nim.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nemo-common.servicename" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
